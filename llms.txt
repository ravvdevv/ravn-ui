# RAVN UI - SaaS Core Design System
Minimal, premium, CDN-first UI library built for high-performance SaaS interfaces. Designed for clarity, scale, and elite aesthetics.

## Overview
RAVN UI provides a strict, production-grade component set with zero framework overhead. It is built on a modular CSS architecture and a robust design token system.

- **Technology**: Vanilla CSS + JS (CDN-First)
- **Size**: < 15KB (Uncompressed), ~7KB (Gzipped)
- **Dependencies**: 0
- **Philosophy**: Neutral palettes, high-end typography (Inter), zero emojis.

## Installation & Setup
### CDN Usage (Recommended)
```html
<link rel="stylesheet" href="https://unpkg.com/@ravn-ui/core/dist/ui.css">
<link rel="stylesheet" href="https://unpkg.com/@ravn-ui/core/dist/themes.css">
<script src="https://unpkg.com/@ravn-ui/core/dist/ui.js"></script>
```

### Package Manager
```bash
bun add @ravn-ui/core
```

## Design Tokens (CSS Variables)
RAVN UI is driven by a 4px base spacing scale and a semantic color palette.
- **Colors**: `--background`, `--foreground`, `--primary`, `--muted`, `--border`.
- **Spacing**: `--space-1` (0.25rem/4px) to `--space-12` (3rem/48px).
- **Radius**: `--radius` (0.375rem).

## Component Library

### Actions & Forms
- **Buttons**: `.btn-primary`, `.btn-outline`, `.btn-ghost`, `.btn-sm`.
- **Inputs**: `.input`, `select`, and `textarea` for theme-aware forms.
- **Toggles**: `.toggle` switch for binary settings.

### Layout & Containers
- **Cards**: `.card` with `.card-header`, `.card-content`, and `.card-footer`.
- **Modals**: `.modal` with `.modal-backdrop`. Toggle via `data-modal-target`.
- **Sidebar**: `.sidebar` with `.sidebar-header`, `.sidebar-nav`, and `.sidebar-item`.
- **Accordions**: `.accordion` with `.accordion-item` and `.accordion-trigger`.

### Data Display
- **Tables**: `.table` with theme-aware hover states.
- **Avatars**: `.avatar` with internal text or image support.
- **Badges**: `.badge` and `.badge-secondary` for status chips.
- **Progress**: `.progress` and `.progress-bar`.
- **Breadcrumbs**: `.breadcrumbs` with `.breadcrumb-item`.

### Feedback & Status
- **Alerts**: `.alert` with `.alert-success`, `.alert-warning`, `.alert-error`.
- **Skeletons**: `.skeleton` with built-in loading animations.
- **Tooltips**: `.tooltip` for contextual hints.
- **Metric Trends**: `.trend-up` and `.trend-down` indicators.
- **Toasts**: Minimal system notifications via `RAVN.showToast()`.

## Theming System
Apply themes via `data-theme` on the `html` tag.
- **Available Themes**: `light`, `dark`, `claude`, `midnight`, `supabase`, `linear`, `zinc`, `forest`, `indigo`, `retro`, `pixel-craft`, `rose-pine`, `nord`.

## JavaScript API (window.RAVN)
- `RAVN.setTheme(string)`: Persists theme selection to localStorage.
- `RAVN.showToast(string)`: Triggers a minimal system toast.
- `RAVN.modal(id, action)`: Programmatic control for modals ('open'/'close').
- `RAVN.copy(text, message)`: Clipboard utility with success feedback.

## PWA & Performance
RAVN UI is optimized for Google Lighthouse 100/100 scores.
- **Offline Support**: Includes a built-in Service Worker and Manifest for PWA installation.
- **Optimization**: Uses `link rel="preload"` and hardware-accelerated animations.

## Documentation
Full documentation available at: https://ravn-ui.vercel.app/
Modular documentation sources: `/docs/components/*.html`
